/IndumentariasOnline-Ventas/ dentro:
-config (dentro config.js con las direcciones)
-src (app.js dentro)
--controllers
--routers
--views
--services
--utils
--models
--repositories
--db (esto lo veremos porque quiero conectarlo a una base como Supadb o mongo, veremos)
-public
--css
--images



Tecnologias:
nodejs
npm
biomejs
express
rest
morgan
ejs (Embeded JavaScript, para pasar datos a planillas html ahora renombradas .ejs)
postgres (para conexion a Supabase)
supabase

A tener en cuenta:
-el server de sql esta a modo de referencia/respaldo, se utiliza unicamente el de supabase.
